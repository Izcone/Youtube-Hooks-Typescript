import styled from 'styled-components';

export const StyledButton = styled.button`
	position: relative;
	right: 30px;
	border: none;
	cursor: pointer;
	background-color: #fff;
	color: #444;
	&:focus {
		outline: inherit;
	}
`;
